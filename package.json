{
  "name": "@project-ncl/pnc-js-libs",
  "version": "2.2.1",
  "description": "Parent package for a collection of PNC JavaScript libraries ",
  "author": "Alex Creasy <acreasy@redhat.com>",
  "license": "Apache-2.0",
  "private": true,
  "devDependencies": {
    "lerna": "^6.0.3",
    "ts-lint": "^4.5.1",
    "typescript": "^4.9.3"
  },
  "scripts": {
    "build": "lerna bootstrap --hoist && lerna run build --stream && lerna run test --stream"
  }
}
