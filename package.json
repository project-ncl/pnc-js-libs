{
  "name": "@project-ncl/pnc-js-libs",
  "version": "2.0.0",
  "description": "Parent package for a collection of PNC JavaScript libraries ",
  "author": "Alex Creasy <acreasy@redhat.com>",
  "license": "Apache-2.0",
  "private": true,
  "devDependencies": {
    "lerna": "^3.18.4",
    "ts-lint": "^4.5.1",
    "typescript": "^3.9.7"
  },
  "scripts": {
    "build": "lerna run build",
    "postinstall": "(cd packages/pnc-dto-types && npm install); (cd packages/pnc-messagebus-client && npm install); (cd packages/umd-bundle && npm install)"
  }
}
